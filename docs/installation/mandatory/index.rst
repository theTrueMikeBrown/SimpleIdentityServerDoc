Mandatory steps
-----------------

There are two different ways to deploy the product, either manually with *MSI installer* or with *Docker*.

Whatever the methodology chosen the following steps are automatically performed,
except the last one (needs to be done if Docker is chosen) an the third (needs to be done if the product is manually installed):

.. toctree::
   :titlesonly:

   dotnet
   redis
   mongo
   kibana
   nodejs
   certificate
