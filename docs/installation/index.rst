Installation
------------

We assumed that all the components are going to be installed on the same machine.
Keep in mind that it's also possible to deploy them on separate servers. It can be useful to do it if you want a load balancing system.

.. toctree::
   :titlesonly:

   mandatory/index
   manual/index
   docker/index
