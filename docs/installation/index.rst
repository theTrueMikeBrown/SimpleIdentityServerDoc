Installation
------------

We assumed that all the components will be installed on the same machine.
Keep in mind that it's also possible to deploy them on separate servers, it can be useful if you want to implement a load balancing system.

.. toctree::
   :titlesonly:

   mandatory/index
   manual/index
   docker/index
